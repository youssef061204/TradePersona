{
  "name": "backend",
  "version": "1.0.0",
  "description": "TradePersona bounded API and canonical Python inference worker",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=20" },
  "overrides": { "snowflake-sdk": { "toml": "^4.2.0" } },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "fast-xml-parser": "^5.3.4",
    "multer": "^2.0.2",
    "snowflake-sdk": "^2.3.3"
  }
}
